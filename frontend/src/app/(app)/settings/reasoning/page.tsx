import { ComingSoon } from "@/components/settings/coming-soon";

export default function ReasoningPage() {
  return (
    <ComingSoon title="Reasoning" milestone="F4.3">
      Toggle the ``&lt;think&gt;`` phase on or off per session, so the model
      skips the internal monologue when you want a straight answer.
    </ComingSoon>
  );
}
