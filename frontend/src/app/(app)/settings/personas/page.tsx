import { ComingSoon } from "@/components/settings/coming-soon";

export default function PersonasPage() {
  return (
    <ComingSoon title="Personas" milestone="F4.2">
      Name, system prompt and per-persona defaults (model, hyperparameters).
      Pick a persona when starting a conversation.
    </ComingSoon>
  );
}
